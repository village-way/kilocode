# Mermaid Diagram Features

**Priority:** P2
**Status:** ❌ Not started

## Remaining Work

- Mermaid diagram rendering in chat messages (currently renders as plain code blocks)
- "Fix with AI" button for mermaid syntax errors — needs to route through CLI
- Copy button for diagram code
- Click to open rendered diagram as PNG in editor
- Error display with original code
- Loading states during processing
